# ===========================================
# HospitalHub - Root / Shared Environment
# ===========================================
# Copy this to .env in each package and fill values

# ============ BACKEND ============
# backend/.env
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/hospitalhub
JWT_SECRET=your-super-secret-jwt-key-change-in-production
CLIENT_ORIGIN=http://localhost:3000
PORT=5000

# AI Service (used by backend)
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini
AI_CACHE_TTL_MS=3600000
AI_RATE_LIMIT_MAX=20

# Optional: n8n webhooks
N8N_WEBHOOK_URL=
N8N_WEBHOOK_TOKEN=

# ============ FRONTEND ============
# frontend/.env.local
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# ============ VERCEL ============
# Set in Vercel dashboard:
# - NEXT_PUBLIC_API_BASE_URL (backend API URL)
# - Backend uses Vercel serverless or separate hosting
